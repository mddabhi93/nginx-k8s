pipeline {
  agent any  
  stages {
    stage ('Build and Test') {
      steps {
        echo "Hello World!"
        sh 'echo Hello from the shell'
        sh 'docker ps'
        sh 'uptime'  
      }
    }  
    
  }
}
